# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependency directories
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Vite cache
.cache/
vite.cache/

# Local environment files (even if not using Vite's .env now, safe to ignore)
.env
.env.local
.env.*.local

# AWS credentials & config (never commit!)
.aws/
*.pem
*.key
credentials.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Optional lock files (comment out if you want to keep them)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
